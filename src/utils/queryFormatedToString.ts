import { ParsedUrlQuery } from 'querystring';

const queryFormatedToString = (query: ParsedUrlQuery) => {
  const queryFormated = '';

  // for (const q of query) {
  //   queryFormated += `&${q}=${q[0]}`;
  // }

  return;
};
